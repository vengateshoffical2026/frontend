// Global feature flag: set to true to show business sections (pricing, checkout, subscriptions)
// Set to false to hide all business-related sections across the app
export const isBusinessMode = true;
